# 한빛생명 상담품질 자동채점 시스템 — 개발 지침서 (claude.md)

> 이 문서는 Claude(바이브코딩)에게 주는 **개발 지침서**다. 채점 프롬프트가 아니다.
> 채점 기준의 원본은 항상 아래 파일이며, 이 문서는 "어떻게 만들지"를 지시한다.
> - 루브릭: 보험상담_평가루브릭_v1_5.md
> - 매칭표: 한빛생명_니즈상품_매칭표_v1_0.md
> - N/A 규칙: N_A_매핑규칙_확정본.md
> - 출력 스키마: 상담평가_출력스키마_ver2.json
> - 골든셋: 골든셋_채점기록표_7건_확정.xlsx (대조·합의 시트)

---

## 1. 프로젝트 개요

### 무엇을 만드나
보험사(가상 "한빛생명") 콜센터의 상담 텍스트를 **전량 자동 채점**하고,
결과를 한눈에 보고 → 위험 건을 찾고 → 상담사 피드백으로 연결하는 웹 도구.

### 왜 필요한가
- **커버리지 문제:** 상담사 대비 평가자가 절대적으로 부족해, 지금은 샘플만
  손으로 채점하고 대부분 상담은 평가 없이 묻힘. 문제가 민원으로 터져야 발견됨.
- **일관성 문제:** 평가 기준이 명문화돼 있지 않아 평가자마다 점수가 갈림.

### 사용자
보험사 QA 평가담당자. 상담사 대규모 인원의 품질을 일관된 잣대로 점검하고
코칭 근거를 뽑아야 하는 사람.

### 핵심 기능 3개
1. **근거 기반 전수 자동 채점 + 교차검증** — LLM이 18항목·100점으로
   채점하고 감점 근거를 제시. 별도 검증 단계가 AI의 잘못된 판단(할루시네이션)을 걸러냄.
2. **채점 결과 목록 + 위험 건 표시** — 저점수·불완전판매 의심 건을 빨간불로
   표시하고, 위험·저점 순으로 정렬해 담당자가 먼저 볼 것부터 검수.
3. **상담사별 대시보드 + 개인 평가 리포트** — 상담사별 평균·약점을 집계하고,
   이름 클릭 시 개인 리포트(스파이더 차트·PDF 다운로드)로 이동.

### 성공 기준 (UX)
- **UX 완결성:** 녹취를 하나씩 듣지 않고, 위험 건 발견 → 원인 확인 →
  피드백 근거 찾기를 한 화면 흐름 안에서 완료.
  (멘토 강조: UX 완결성이 정확도보다 우선하는 성공 기준)

> 정확도(골든셋 80% 일치)는 완성 후 검증 항목 → 10장 참조.

---

## 2. 기술 스택

### 확정 (반드시 이걸로)
- **프론트엔드 + 엔진:** Next.js 하나로 (React 기반. 라우팅 내장·Vercel 배포 용이·API 키를 서버측에 숨김)
  - **채점 엔진은 Next.js API 라우트에 둔다** (별도 파이썬 서버 없음 — 팀 통합 결정)
  - 언어: **TypeScript**
- **실행 환경:** Docker (docker-compose: app · mysql · chroma 3서비스)
- **코드 저장:** GitHub (`qa-scope`)
- **벡터 DB (RAG용):** Chroma (JS 클라이언트)

### 갈아끼우는 부분 (설정값으로 분리 — 3장 상세)
아래 4가지는 특정 기술에 고정하지 말고, 독립 함수로 감싸 설정으로 교체 가능하게 짤 것.

| 부분 | MVP 기본값 | 나중 교체 대상 |
|---|---|---|
| LLM API | Gemini 무료 (`gemini-2.5-flash`, 대량 테스트 시 `flash-lite`) | Claude 등으로 교체 |
| 참조자료 공급 | 문서 통째로 프롬프트 삽입 | Chroma RAG 검색 (2단계에서 교체) |
| 결과 저장 | MySQL (docker-compose로 처음부터 — 우현 설계 채택) | 배포 시 호스팅형 DB로 이전 검토 |
| 입력 소스 | 텍스트(타임스탬프+화자분리) | 앞단에 STT 추가 |

> 원칙: LLM·저장소·검색·입력은 "콘센트처럼" 밖으로 분리. 엔진 코어는 이들이
> 바뀌어도 안 바뀌게 유지. (배포·비용·보안 상황 변화에 대응)

### 참고 (착수 시점에 결정)
- LLM 최종 제공자: 공모전 Claude 크레딧 제공 여부 확인 후 결정 가능.
  (프로바이더 비교 — 일치율·비용·지연 — 후 기본값 결정: 우현 §16)
- 배포: MVP·자체 실테스트 이후. 단, 배포 가능한 구조로 개발 (Vercel Hobby 무료
  플랜은 비상업 데모까지 허용 — 공모전 시연 OK).

---

## 3. 아키텍처 & 모듈 분리

### 전체 흐름
입력(상담 텍스트, MySQL) → ① 상담 유형 분류 → ② 채점 엔진(채점 → 3중 검증)
→ ③ 결과(JSON) → 저장(MySQL) → 화면
(엔진·화면 모두 Next.js 안: 엔진=API 라우트, 화면=페이지)

### 핵심 설계 원칙: 엔진 코어와 어댑터 분리
**채점 엔진 코어(유형분류·채점·교차검증)는 고정**하고, 외부와 닿는
부분은 전부 독립 함수(어댑터)로 감싼다. 어댑터를 교체해도 엔진은
수정하지 않는다.

### 어댑터 4개 (반드시 분리)
| 어댑터 | 역할 | 함수(예시) | MVP 기본값 → 교체 대상 |
|---|---|---|---|
| 입력 | 상담기록 읽어오기 | `load_transcript()` | 텍스트 → 앞단 STT 추가 |
| 참조자료 | 약관·상품설명서·매칭표 공급 | `retrieve_context()` | 통째로 삽입 → Chroma RAG |
| LLM | AI 채점 호출 | `call_llm()` | Gemini → Claude 등 |
| 저장 | 결과 저장·조회 | `save_result()`·`load_results()` | MySQL (어댑터 분리는 유지) |

### 개발 규칙
- 위 4개 함수의 **입출력 형태(인터페이스)를 먼저 고정**하고, 그 안의 구현만 바꾼다.
  예: `call_llm(prompt) → 텍스트` 형태는 불변, 내부에서 어느 API를 부르는지는 설정값.
- 엔진 코어 코드에는 특정 API 이름(Gemini/Claude)·특정 DB 이름을 직접 쓰지 않는다.
  항상 어댑터 함수를 통해서만 호출한다.
- 함수 이름은 예시다. 역할 분리만 지키면 명명은 자유.
- 입력 형태는 항상 **"타임스탬프 + 화자분리된 텍스트"**로 통일 (7장).
- 교차검증(2-pass)은 엔진 코어의 일부다. on/off 스위치를 만들지 않는다.

---

## 4. 개발 단계 (이 순서대로)

**대원칙: "돌아가는 것"이 우선. UI 디자인(꾸미기)은 맨 나중.**
각 단계는 "동작 확인 → 다음"으로. 한꺼번에 다 만들지 않는다.

### 0단계 — 준비 (완료)
루브릭 v1.5·매칭표·N/A규칙·골든셋 7건·JSON 스키마 확보 완료.

### 1단계 — 채점 엔진 코어 (화면 없이)
- 참조자료: 문서 통째로 프롬프트 삽입 (RAG 아직 아님)
- LLM: 무료 Gemini / 저장: MySQL (docker-compose 시드에 더미 7건 포함)
- 상담 텍스트 1건 → 유형분류 → 채점(2-pass) → JSON 결과
- **검증:** 골든셋 7건으로 사람 채점과 비교 (10장 방식)
- ※ 이 단계에서 엔진 언어(파이썬/JS) 결정

### 2단계 — RAG 교체 (Chroma)
- `retrieve_context()` 어댑터만 "통째로 삽입" → "Chroma 검색"으로 교체
- 엔진 코어는 안 건드림. 교체 후 골든셋 재검증(점수 동일해야 정상)

### 3단계 — 화면 (기능 위주, 꾸미기 X)
- Next.js로 화면 4개: 목록·상세·대시보드·개인리포트 (9장)
- **작업 순서: ①목록+②상세 먼저**(담당자 UX의 축 — 우현 §10) → ③대시보드 → ④리포트
- **스타일링(색·폰트·여백 등 미관)은 최소한만.** 흐름이 도는 것만 확인

### 4단계 — 자체 실테스트
- 골든셋 외 더미도 넣어보고, 화면 흐름 전체를 팀이 직접 점검
- 멘토 지시: "실 테스트를 자체적으로 잘 해놓고"

### 5단계 — 배포 + 기업담당자 컨펌
- 배포 환경 구성(MySQL·Chroma 호스팅 포함) 후 배포(URL)
- 멘토 지시: "배포 후 링크로 공유" → "기업담당자한테 컨펌 최소 1~2번"

### 6단계 — UI 다듬기 + 컨펌 반영
- 기업담당자 피드백 반영 + UI 미관 개선 → 발표용 완성도

### 확장 (여유 시)
- 입력 앞단 STT / 관리자용 평가기준 입력 화면

---

## 5. 채점 로직 사양

### 5.1 참조자료 배치 (하드프롬프팅 vs RAG)

| 자료 | 파일 | 공급 방식 | 이유 |
|---|---|---|---|
| 루브릭 v1.5 | 보험상담_평가루브릭_v1_5.md | **하드**(프롬프트에 전체 삽입) | 채점 잣대 자체. 매 채점마다 18항목 전부 필요 |
| 니즈-상품 매칭표 | 한빛생명_니즈상품_매칭표_v1_0.md | **하드** | D2 정답지. 작고(약4.5천자), 전체를 봐야 판정 가능 |
| 가상약관 | 한빛생명_가상약관_v2_0.md | **RAG**(관련 부분만 검색) | 큼. 상담마다 해당 상품·조항만 필요 |
| 상품설명서 | 한빛생명_상품설명서_v1_0.md | **RAG** | 큼. 상담마다 해당 상품만 필요 |

**개발 규칙**
- 하드 자료(루브릭·매칭표)는 채점 프롬프트에 **항상 전체**가 들어간다.
- RAG 자료(약관·상품설명서)는 `retrieve_context()` 어댑터가 상담 내용과
  관련된 부분만 검색해 프롬프트에 추가한다.
- 1단계(MVP)에선 RAG 자료도 임시로 전체 삽입 가능. 2단계에서 Chroma 검색으로 교체.
  (배치 원칙은 유지 — 루브릭·매칭표는 어느 단계든 하드)

> 주: 매칭표를 하드로 두는 것은 PRD(매칭표=RAG 3종 포함)와 다른 확정 결정.
> 근거: 매칭표는 D2 정답지라 전체가 필요하고 작아서, RAG로 쪼개면 오히려
> 검색 누락 → 판정 오류 위험. (팀 결정 2026-07-02)

### 5.2 AI 판단 vs 코드 계산 분리 (정확도의 핵심)

원칙: **판단(주관)은 AI가, 계산(규칙)은 코드가.** 산수·환산·컷 판정을 AI에 맡기지 않는다.

**AI가 하는 것 (판단)**
- 상담유형·권유유형 분류 (+근거)
- 각 항목 충족수준 판정: 충족 / 부분충족 / 미충족 / 해당없음(N/A)
  (+ 원문 그대로 인용한 근거, 짧은 코멘트)
- D2 확인절차 수행 여부(3개 boolean) · 매칭판정(적합/부적합/명백한 오류) 추출
- 위험플래그 조건 식별 (+근거)

**코드가 하는 것 (계산)**
- 배점 대입 (아래 고정표 — AI가 배점을 정하지 않음)
- 획득점수: 충족=배점 / 부분충족=배점÷2 / 미충족·해당없음=0
- N/A: 적용배점합(분모)에서 그 배점 제외
- 집계: 원점수합 · 적용배점합 · 환산총점 (8장)
- D2 3×3 게이트 적용 (5.4)
- 위험표시여부 · 상태라벨 (8장)

**배점 고정값 (루브릭 v1.5 — 코드가 이 값을 대입)**

| 영역 | 항목 | 배점 |   | 영역 | 항목 | 배점 |
|---|---|---|---|---|---|---|
| A (12) | A1 | 4 |   | D (30) | D1 | 7 |
|  | A2 | 4 |   |  | D2 | 5 |
|  | A3 | 4 |   |  | D3 | 5 |
| B (26) | B1 | 8 |   |  | D4 | 4 |
|  | B2 | 6 |   |  | D5 | 4 |
|  | B3 | 6 |   |  | D6 | 5 |
|  | B4 | 6 |   | E (14) | E1 | 7 |
| C (18) | C1 | 6 |   |  | E2 | 7 |
|  | C2 | 6 |   |  |  |  |
|  | C3 | 6 |   | **합계** |  | **100** |

- 5점 항목(D2·D3·D6)의 부분충족 = 2.5점 (소수 그대로 합산)

**왜 이렇게 나누나:** AI에 산수를 맡기면 값이 들쭉날쭉해져 골든셋 일치율이 깨진다.
AI는 "충족수준(범주)"까지만 판단하고, 숫자 계산은 코드가 매번 똑같이 처리한다.

### 5.3 루브릭 적용 방식

**(1) 판정 방법**
- 루브릭 v1.5 전체를 프롬프트에 로드. 각 항목의 "AI가 확인할 것" + 앵커
  예시를 기준으로 충족수준을 판정.
- 근거는 상담 원문을 **그대로 인용**(요약·각색 금지). 인용문에 대화ID를 달아
  "점수 클릭 → 원문 점프"가 되게 함.
- **각 항목은 그 항목 근거만으로 판정한다. 상담 유형 라벨이나 전체 인상으로
  개별 점수를 흔들지 않는다.**

**(2) 상담 유형 분류 — 채점 전 먼저 (단계 ①.5)**
- 채점 시작 전 AI가 먼저 판단: 5종 유형(신규·보장 / 계약변경 / 해지·환급 /
  보험금청구 / 단순문의) + **"신규 권유/판매가 있었나"** (→ N/A 결정)
- 입력 데이터에 유형이 이미 있으면 그걸 우선 사용. 없으면 AI가 분류.

**(3) N/A(해당없음) 규칙** — 단일 기준: N_A_매핑규칙_확정본.md
핵심: D영역은 유형 라벨이 아니라 **"실제로 권유/판매가 있었나"**로 켠다.

- **2단계 트리거**
  - D1~D5(설명·적합성 의무): **신규 상품 판매/권유**가 있을 때만 ON
  - D6(부당권유 금지): 신규뿐 아니라 **유지·해지방어·승환 등 어떤 권유든**
    있으면 ON. 아무 권유도 없으면 D6도 N/A
- **항목 3분류**
  - 항상 채점(10): A1 A2 A3 B1 B3 B4 C1 C2 E1 E2
    · 단 A2는 "내 계약 안 건드리는 순수 일반문의"일 때만 N/A
  - 상황 조건부(2): B2(전문용어를 실제로 썼을 때) · C3(고객이 감정 표출했을 때)
  - 판매전용(6): D1~D5(신규 권유 시) · D6(어떤 권유든 있으면 항상)
- **N/A의 뜻:** "평가 상황 자체가 없었음". 문제가 없던 것은 N/A가 아니라 **충족.**

**(4) 경계 규칙 — 중복감점 금지 (사안별 귀속)**

| 사안 | 귀속 항목 | 주의 |
|---|---|---|
| 안내한 정보가 틀림(부정확) | **B1** | 누락과 별개 |
| 4대 필수항목 누락 | **D1** | 부정확과 별개. 한 사안에 둘 다면 각각 독립 감점 |
| 부적합 상품인데 설명은 정확 | B1=충족, 부적합은 **D2** | 부적합으로 B1 깎지 않음 |
| 고객 니즈를 결국 파악했는가 | **A3**(결과만) | 태도→C2·B3, 상품적합성→D2 |
| 고객이 되묻게 된 원인 | **B3·B2·A3·E1** | B4 아님 |
| 상담사 측 반복·돌려막기·무단보류 | **B4** | 고객 측 반복은 위 참조 |

### 5.4 D2 적합성 채점 (3×3 게이트)

D2는 **"물어봤는가(절차)"와 "맞는 상품을 줬는가(결과)"를 모두** 본다.
(신규 판매 권유가 있을 때만 평가, 없으면 N/A)

**AI가 판단해서 판매정보에 채우는 값**
- 권유상품코드 (P1~P7) · 니즈시나리오코드 (N1~N8, 판단불가 시 null)
- **매칭판정(결과 축):** 매칭표에 비춰 적합 / 부적합 / 명백한 오류
- **확인절차(절차 축):** 가입목적 · 재정상황 · 기존계약 각각 true/false
- 판정 근거 (원문 인용)

**코드가 계산하는 값 (3×3 게이트)**
확인절차의 true 개수(3 / 1~2 / 0)와 매칭판정을 조합:

| 절차＼결과 | 적합 | 부적합(무해) | 명백한 오류(유해) |
|---|---|---|---|
| **3개 확인** | 충족 (5점) | 부분충족 (2.5) | 미충족 (0) + 위험플래그 |
| **1~2개** | 부분충족 (2.5) | 부분충족 (2.5) | 미충족 (0) + 위험플래그 |
| **0개(절차 전무)** | 미충족 (0) | 미충족 (0) | 미충족 (0) + 위험플래그 |

**핵심 규칙**
- 개수만 센다 (항목별 가중 없음 — 재현성 우선)
- 절차 0개면 결과가 적합이어도 미충족 (우연히 맞춘 건 인정 안 함)
- 명백한 오류(유해)는 절차와 무관하게 미충족 + 위험플래그(규칙6) 동반
- 매칭판정의 정답지는 **매칭표**다 (5.1에서 하드로 로드)

---

## 6. 채점 절차 (상담 1건 처리 — 3중 안전망)

**계산은 판정이 최종 확정된 뒤 맨 마지막에 한 번만.**

### 1차 패스: 채점
① 참조자료 확보: 루브릭·매칭표(하드) + 약관·상품설명서(RAG) 로드
①.5 유형 분류: 5종 유형 + "신규 권유/판매 있었나" 판단 (→ N/A 결정)
② 항목 채점: 적용 항목만 충족수준 판정 (+원문 인용 근거)
   · D2는 확인절차(3 boolean)·매칭판정 추출 / 위험플래그 조건 식별

### 안전망 1 — 형식 검증 (코드)
③ LLM 응답을 스키마 ver2로 검증 (코드펜스·머리말 제거 후 파싱).
   실패 시 재시도, 반복 실패는 **사람 검토 큐**로. (우현 §9 채택)

### 안전망 2 — 인용 대조 (코드, LLM 비용 0)
④ 각 근거의 인용문이 **원문 transcript에 실제 존재하는 문자열인지** 코드로 대조.
   불일치(지어낸 인용) 발견 시 해당 항목을 재판정 대상으로 표시.

### 안전망 3 — 내용 교차검증 = 2차 패스 (LLM)
⑤ 별도 검증 호출이 1차 결과를 점검 (멘토 지시 — 코어 고정, 스위치 없음):
   · N/A 처리가 권유 유무와 맞는가
   · 매칭판정이 매칭표와 어긋나지 않나
   · 인용이 항목 판정의 근거로 타당한가
⑥ 불일치 항목 재판정 → 판정 최종 확정

### 계산 (코드 — 확정된 판정으로)
⑦ 배점 대입 → 획득점수 → D2 게이트 → 집계(환산총점) → 플래그·라벨 (8장)
⑧ JSON 출력 → MySQL 저장 (원본 JSON + 항목별 정규화: 우현 §6)

> 근거: 멘토 — "모니터링 검증 AI를 별도로 만들어 서로 검증해야 할루시네이션을
> 줄이고 성능을 보장." / 계산을 맨 뒤에 두는 이유: 재판정으로 판정이 바뀌면
> 중간 계산은 폐기되므로. / 재현성: temperature 낮게, 모델명은 앱이 평가메타에 주입.

---

## 7. 입출력 사양

### 7.1 입력 — 상담 텍스트(transcript)
통짜 텍스트가 아니라 **발화 단위로 분리된** 구조로 받는다.

| 필드 | 설명 |
|---|---|
| 대화ID | 발화 하나의 고유번호(PK). 근거 인용·"원문 점프"의 연결고리 |
| 상담ID | 어느 상담에 속하는지 |
| 대화순서 | 1, 2, 3… 상담 내 순서 |
| 화자 | 상담사 / 고객 |
| 타임스탬프 | 발화 시작 시각(초 단위) |
| 대화내용 | 실제 발화 문자열 |

- **화자 분리가 핵심:** 같은 말도 화자에 따라 채점이 달라지고,
  "상담사 응대만 평가" 지시가 가능해진다.
- ※ STT 엔진이 이 분리를 이미 제공. 통짜 과거데이터 전처리는 확장 항목.

### 7.2 출력 — JSON (스키마 ver2 준수)
`상담평가_출력스키마_ver2.json`을 **단일 기준**으로 준수. 주요 블록:
- 상담ID · 평가메타(평가주체·AI모델·루브릭버전·평가일시)
- 분류(상담유형·권유유형 +근거)
- 판매정보(신규판매 시 객체 / 그 외 null): 권유상품·매칭판정·확인절차
- 항목평가(18): 항목코드·충족수준·배점·획득점수·근거(대화ID+인용문)·코멘트
- 위험플래그(규칙번호·관련항목·근거)
- 집계(원점수합·적용배점합·환산총점·위험표시여부·상태라벨)
- 고객만족

### 7.3 입력 ↔ 출력 연결 (중요)
- **항목평가.근거.대화ID = 입력 transcript의 대화ID.** 이 연결로 화면에서
  "점수 클릭 → 해당 발화로 점프"를 구현한다.
- 엔진과 화면은 **이 JSON으로만 대화**한다. 형식만 지키면 엔진 언어(파이썬/JS)는
  자유 — 엔진 언어 결정이 화면에 영향을 주지 않는 이유.

---

## 8. 점수·라벨·위험플래그 규칙 (전부 코드가 계산)

### 8.1 점수 집계
- 원점수합 = N/A 아닌 항목들의 획득점수 합
- 적용배점합 = 100 − (N/A 항목 배점 합)  ← 환산 분모
- 환산총점 = round(원점수합 ÷ 적용배점합 × 100, 1)

### 8.2 위험플래그 6종 (AI가 조건 식별 → 코드가 부여)
| 규칙 | 조건 | 주의 |
|---|---|---|
| 1 | D6 위반 (부당권유) | |
| 2 | D1 핵심누락 | **원금손실이 실재하는 상품만**(저축성·종신). 보장성(건강보험 등) 제외. D1 미충족이 자동으로 규칙2를 켜지 않음 |
| 3 | B1 오정보 | |
| 4 | C3 미충족 + 강한 불만 (민원위험) | |
| 5 | D영역 득점률 < 50% | **D1~D5가 적용된 신규판매 상담만.** D 전부 N/A면 분모 0 → 건너뜀 |
| 6 | D2 명백한 오류 | 5.4 게이트와 연동 |

### 8.3 상태라벨 (JSON엔 1개 저장)
- 플래그 1·2·5·6 중 하나라도 → **불완전판매 의심**
- 환산총점 < 70 → **저점수**   ※ 70은 설정 파라미터로 (하드코딩 금지)
- 그 외 → **정상**
- 둘 다 해당 시 저장 우선순위: 불완전판매 의심 > 저점수
  (화면 표시는 병기 — 9장. "불완전판매 의심"을 앞에. v14 팀 결정)

### 8.4 위험표시여부 (라벨과 별개의 안전망)
- 위험플래그가 **1개라도** 있으면 true → 목록에서 빨간 위험 아이콘
- 존재 이유: 플래그 3·4만 있고 총점 70 이상이면 상태라벨은 "정상"인데,
  이때도 위험 아이콘은 켜져서 **초록불에 묻히지 않게** 한다.

---

## 9. 화면 사양 (Next.js, 4개 화면)

**대원칙(4장 재확인): 기능이 도는 것 우선. 미관은 6단계에서.**
**범위는 4화면 확정** (③④는 PRD 핵심기능 3 — "후속"으로 미루지 않는다).
화면 ①②③은 목업 3장(스크린샷_20260616_*.png)의 **기능 구성을 따른다.**
레이아웃·배치는 개선 가능 — 단 기능 요소(필터·위험표시·점프·병기 등) 누락 금지. ④는 신규.

**흐름 대칭:** ①목록 →(보기)→ ②상세(상담 1건) / ③대시보드 →(이름 클릭)→ ④리포트(상담사 1명)

### 화면 ① 채점 결과 목록
- 상단 탭: [채점 결과 목록] [채점 상세] [상담사별 대시보드]
- 필터: 날짜·상담사·유형·점수 낮은 순
- 표 컬럼: 상담번호·상담사·유형·총점·상태
- **위험 건 = 빨간 배경.** 위험표시여부 true면 상태라벨이 "정상"이어도
  빨간 위험 아이콘 (8.4)
- 상태라벨 병기: 둘 다 해당 시 둘 다 표시, "불완전판매 의심"을 앞에
- 각 행 "보기" → 화면 ②. 하단 요약: "전량 N건 채점 완료 · 위험 N건"

### 화면 ② 채점 상세
- 헤더: ← 목록으로 / 상담번호·상담사·유형·위험태그
- **좌측:** 상담 원문(타임스탬프 포함) / **우측:** 항목별 점수 + 총점
- **핵심 기능: 우측 항목 클릭 → 좌측 근거 발화로 점프** (대화ID 매칭 — 7.3)
- 하단: 검수확정 · 점수수정 · 코멘트

### 화면 ③ 상담사별 대시보드
- 상단 요약 4개: 전체평균 · 채점건수 · 위험건 · 상담사 수
- 표: 상담사 · 건수 · 평균점수(막대) · 위험 · 약점항목
- 상담사 이름 = 클릭 링크 → 화면 ④. 하단: 코칭 연결 문구

### 화면 ④ 상담사 개인 평가 리포트 (신규 — 목업 없음)
- 헤더: ← 대시보드로 / 상담사명 · 평가기간 · 총 평가건수
- **스파이더 차트:** 5영역(A~E) 점수 시각화
- 항목별 점수 상세 / 개선 필요 항목(하위 항목 + 위반 횟수)
- [PDF 다운로드] 버튼

---

## 10. 검증 방법 (엔진이 맞게 채점하는지)

### 10.1 기준: 골든셋 7건
사람이 손으로 채점한 정답 = `골든셋_채점기록표_7건_확정.xlsx` (대조·합의 시트).

| 더미 | 사람 채점(환산) | 성격 |
|---|---|---|
| #1 | 100 | 우수 |
| #2 | 32.4 | 불량 |
| #3 | 49.2 | 불량 |
| #4 | 100 | 우수 |
| #5 | 94 | 우수 |
| #6 | 27.9 | 불량 |
| #7 | 65.9 | 중간 |

### 10.2 절차
1. 1단계(엔진) 완료 시: 더미 7건을 엔진에 넣고 AI 채점 실행
2. AI 결과 vs 골든셋을 **항목 단위**로 대조 (총점만 보지 않는다)
3. 목표: **일치율 80% 이상** (PRD 성공기준 ①)
4. 2단계(RAG 교체) 후 재실행 → 점수가 1단계와 같아야 정상

### 10.3 일치율 정의 (팀 확정)
- **주 지표 — 항목 단위:** AI 충족수준 == 사람 충족수준 (4택 비교).
  일치율 = 일치 항목 수 ÷ 전체 적용 항목 수 (7건 합산). 목표 80%.
- **보조 지표 — 총점 오차:** 건별 |AI 총점 − 사람 총점| 참고.
  (총점만 보면 틀림끼리 상쇄되어 착시 발생 — 주 지표로 쓰지 않는 이유)
- 불일치 항목은 목록화 → 원인 분석(프롬프트 보강 등) → 재실행

### 10.4 필수 확인 케이스 (알려진 구멍 — 설계는 완료, 실동작 확인 필요)
- **총점 70 이상 + 플래그 3(오정보) 또는 4(민원위험)만 있는 건:**
  상태라벨 "정상"이어도 위험표시여부=true로 빨간 아이콘이 뜨는지 확인.
- D 전체 N/A인 건(단순문의류): 규칙5가 분모 0으로 건너뛰는지 확인.
- 라벨 병기 건: 불완전판매 의심 + 저점수 동시 해당 시 화면 표시 확인.

### 10.5 골든셋 무결성 (절대 규칙)
- 골든셋 점수·매칭표는 **AI 결과를 본 뒤 수정 금지.**
  (수정하면 80% 일치율이 무의미해짐)
- AI가 틀렸을 때 고치는 건 골든셋이 아니라 **프롬프트·로직**이다.

---

## 11. 리포지토리 구조 (제안 — 우현 §4 채택)

```
/
├─ CLAUDE.md                  ← 이 파일(개발 지침서)
├─ docker-compose.yml         ← app · mysql · chroma
├─ .env.example               ← 키·컷값 등 설정 템플릿 (실제 .env는 커밋 금지)
├─ prompts/scoring-prompt.md  ← 채점 프롬프트
├─ schemas/
│   ├─ 상담평가_출력스키마_ver2.json
│   └─ 상담평가_출력예시_해지환급.json  ← 참고용 샘플
├─ docs/
│   ├─ hard/                  ← 프롬프트 인라인 전용 (RAG 인덱싱 절대 금지)
│   │   ├─ 보험상담_평가루브릭_v1_5.md
│   │   ├─ 한빛생명_니즈상품_매칭표_v1_0.md
│   │   └─ N_A_매핑규칙_확정본.md
│   ├─ rag/                   ← Chroma 인덱싱 대상
│   │   ├─ 한빛생명_가상약관_v2_0.md
│   │   └─ 한빛생명_상품설명서_v1_0.md
│   └─ mockups/               ← 화면 ①②③ 목업 (3단계 UI 참고)
│       ├─ 스크린샷_20260616_154752.png
│       ├─ 스크린샷_20260616_154839.png
│       └─ 스크린샷_20260616_154847.png
├─ app/                       ← Next.js (화면 + API 라우트=엔진)
├─ lib/
│   ├─ scoring/               ← 파이프라인: 로드→RAG→LLM→3중검증→계산
│   ├─ llm/                   ← 프로바이더 추상화 (gemini/claude)
│   ├─ rag/                   ← Chroma 인덱싱·검색
│   └─ db/                    ← MySQL 접근
├─ scripts/                   ← 인덱싱·시드(더미7건)·골든셋 회귀 실행
└─ test/golden/
    ├─ golden_set.json        ← 합의판정 정답 파일 (대조·합의 시트 추출본, 검증 코드가 읽음)
    └─ transcripts/           ← 더미 상담 원문 7건 (엔진 입력)
        └─ dummy_01.txt ~ dummy_07.txt
```

---

## 12. 정본 문서 & 버전 정합 (우현 §5 채택)

코드·프롬프트·스키마가 참조하는 정본. **서로 어긋나면 채점이 흔들린다.**

| 문서 | 버전 | 역할 |
|---|---|---|
| 평가 루브릭 | v1.5 | 18항목 100점, 앵커·판정 기준 |
| 출력 JSON 스키마 | ver2 | 채점 결과 계약 |
| 니즈-상품 매칭표 | v1.0 | D2 결과 축 고정 정답지 |
| 가상약관 | v2.0 | D영역·B3 법적 근거 |
| 상품설명서(팩트시트) | v1.0 | B1 수치 근거. **수치 상충 시 약관보다 우선** (약관 v2.0 머리말 명시) |
| N/A 매핑 규칙 | 확정본 | D 트리거·조건부·유형 분류 |
| 골든셋 | 7건 정본 xlsx | 사람 손채점 (회귀 기준) |

- 문서 갱신 시 버전 번호와 참조를 함께 올린다. 이력은 지우지 않고 누적 보존.

---

## 13. 보안 & 코딩 원칙 (우현 §12 채택)

- **시크릿 금지:** API 키·DB 비밀번호를 코드/커밋에 넣지 않는다. 전부 `.env`.
- **PII:** 고객 개인정보는 마스킹/가명화. 로그에 원문·개인정보 남기지 않기.
- **설정 외부화:** 70점 컷·프로바이더·모델명·임계값은 설정으로 (하드코딩 금지).
- **재현성 우선:** 영리하지만 재현 안 되는 로직보다 단순·검증 가능한 로직.
  채점 LLM 호출은 temperature 낮게.

---

## 14. 하지 말 것 (Do NOT — 우현 §15 채택·보강)

- ❌ 매칭표를 채점 결과에 맞춰 수정 (자기충족 — 일치율 무의미화)
- ❌ AI로 골든셋 점수 생성 / 우수·미흡 라벨을 채점 입력에 노출
- ❌ 집계·상태라벨을 LLM 출력값 그대로 신뢰 (반드시 코드 재계산)
- ❌ 70점 컷 하드코딩
- ❌ 고정 정답지(매칭표·루브릭·N/A)를 RAG 검색으로 대체
- ❌ docs/hard/ 파일을 Chroma에 인덱싱 (Chroma 대상은 docs/rag/ 만)
- ❌ 교차검증(2-pass)에 on/off 스위치 추가
- ❌ 시크릿·PII를 코드/커밋/로그에 노출
- ❌ 문서 버전 불일치 방치

> 매칭표·골든셋·집계·버전은 신뢰의 근간. 코딩 에이전트가 이 넷을 건드리는
> 변경을 제안할 때는 반드시 사람 확인을 받는다. (우현 §13)

---

## 15. 검증 보강 — 경계 케이스 회귀 (우현 §14 채택)

10장 골든셋 검증에 아래 케이스를 회귀 픽스처로 고정:
- **#1** 절차 3개 + 적합 → D2 충족 (게이트 좌상단)
- **#2** N8→P1 명백한 오류 → D2 미충족 + 플래그6 (게이트 우열)
- **#7** 절차 1개 + 적합 → D2 부분충족 (게이트 중간행)
- N/A 트리거 (권유 유무별 D 영역 on/off)
- 10.4의 필수 확인 케이스 (위험표시여부·규칙5 분모0·라벨 병기)
