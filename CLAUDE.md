# CLAUDE.md — 한빛생명 콜센터 QA 자동채점 시스템

> **이 파일은 무엇인가:** 리포지토리 루트에 두는 **프로젝트 가이드**다. Claude Code 등 AI 코딩 에이전트와 팀원이 프로젝트의 목적·구조·규칙·불변식을 한눈에 파악하도록 한다. **코드 작성 전 이 문서를 먼저 읽는다.**
> **저장 위치:** 리포 루트 `/CLAUDE.md`.
> **미정 영역:** 세부가 확정되지 않은 부분은 "🔓 열림"으로 표시했다. 열린 항목은 자유롭게 제안·구현하되, "🔒 불변" 규칙은 임의로 바꾸지 않는다(바꾸려면 팀 합의).

---

## 1. 프로젝트 개요

가상 생명보험사 **한빛생명**의 콜센터 상담(STT 텍스트)을 **AI가 자동 채점**하고, **QA 담당자가 웹에서 검토·관리**하는 시스템이다.

- **사용자:** 콜센터 QA(품질관리) 담당자. → UX는 "많은 상담 중 문제 건을 빠르게 찾아 근거와 함께 확인"에 최적화.
- **성공지표:** AI 채점 ↔ 사람 골든셋 **80% 일치율**.
- **개발 기조(멘토 방향):** **기능 구현 우선, MVP 먼저.** 더미 정교화·기준 미세조정은 후순위. 담당자가 나중에 골든셋·컷값·기준을 조정할 여지를 열어둔다. (MVP → 기업 컨펌 → 실테스트 → 배포)
- **채점 기준:** 5영역 / 18항목 / 100점 루브릭 (§5).

---

## 2. 기술 스택

| 층 | 기술 | 비고 |
|---|---|---|
| UI / 서버 | **Next.js** | App Router 가정(🔓). 프론트 + API 라우트(BFF)에서 채점 오케스트레이션. |
| 개발/실행 환경 | **Docker** | app · mysql · chroma 컨테이너를 compose로. |
| 정형 데이터 | **MySQL** | 상담 마스터 / 상세 대화 내역 / 평가 결과 (§6). |
| 벡터 검색(RAG) | **ChromaDB** | 상품설명서·약관 사실 조회 (§8). |
| LLM API | **Claude 또는 Gemini** | 프로바이더 **교체 가능**하게 추상화 (§9). |

> 언어·런타임·패키지 매니저·ORM·테스트 러너 등은 🔓 열림. TypeScript 권장.

---

## 3. 시스템 아키텍처 & 데이터 흐름

```
[STT 엔진]  화자·순서·타임스탬프로 분리된 상담 텍스트
     │  (과거 통짜 텍스트는 별도 전처리 에이전트로 화자 분리 — 향후)
     ▼
[MySQL]  상담 마스터 + 상세 대화 내역(발화 단위, 대화ID = PK)
     │
     ▼
[채점 파이프라인]  Next.js API 라우트
     │   1) 상담 1건 로드(발화 배열)
     │   2) 필요한 사실만 ChromaDB에서 검색(B1·D 근거)   ← §8
     │   3) 채점 프롬프트 + 상담 + RAG 사실 → LLM 호출     ← §9
     │   4) LLM 응답 JSON 파싱·스키마 검증(ver2)           ← §5
     │   5) ★ 집계·상태라벨을 앱이 결정론적으로 재계산      ← §7 (LLM 산수 미신뢰)
     ▼
[MySQL]  평가 결과 저장(원본 JSON + 항목별 정규화)
     │
     ▼
[프론트엔드]  목록(상태라벨·저점수 정렬) → 상세(점수 클릭 → 원문 점프)  ← §10
```

핵심: **LLM은 `항목평가`(항목별 충족수준·근거)까지만 신뢰 입력으로 받고, 점수 합산·환산·플래그·상태라벨은 앱 코드가 계산한다.**

---

## 4. 리포지토리 구조 (제안 — 🔓 확정 아님)

```
/
├─ CLAUDE.md                 ← 이 파일(프로젝트 가이드)
├─ docker-compose.yml
├─ .env.example              ← 키·컷값 등 설정 템플릿(실제 .env는 커밋 금지)
├─ prompts/
│   └─ scoring-prompt.md      ← 채점 프롬프트
├─ schemas/
│   └─ eval-output.ver2.json  ← 출력 JSON 스키마(정본)
├─ docs/                       ← 루브릭·매칭표·약관·상품설명서·N/A 규칙(정본 사본)
├─ app/ 또는 src/              ← Next.js (라우트·페이지·컴포넌트)  🔓
├─ lib/
│   ├─ scoring/               ← 파이프라인: 로드→RAG→LLM→검증→재계산
│   ├─ llm/                   ← 프로바이더 추상화(claude/gemini)
│   ├─ rag/                   ← ChromaDB 인덱싱·검색
│   └─ db/                    ← MySQL 접근
├─ scripts/                   ← 인덱싱·시드·골든셋 회귀 실행
└─ test/
    └─ golden/                ← 골든셋 7건 회귀 픽스처
```

---

## 5. 정본 문서 & 버전 (🔒 버전 정합 필수)

코드·프롬프트·스키마가 참조하는 **정본과 버전**. 서로 어긋나면 채점이 흔들린다.

| 문서 | 버전 | 역할 |
|---|---|---|
| 평가 루브릭 | **v1.5** | 5영역 18항목 100점, 앵커·판정 기준 |
| 출력 JSON 스키마 | **ver2** | 채점 결과 계약(`판매정보.확인절차` 신설) |
| 니즈-상품 매칭표 | **v1.0** | D2 결과 축 **고정 정답지** |
| 가상약관 | **v2.0** | D영역·B3 법적 근거(보험계약대출 포함) |
| 상품설명서(팩트시트) | 최신 | B1 수치 정확성 근거. **수치 상충 시 약관보다 우선.** |
| N/A 매핑 규칙 | 확정본 | D 트리거·조건부 항목·유형 분류 |
| 골든셋 | 7건 정본 xlsx | 사람 손채점 결과(회귀 기준) |

> 문서 갱신 시 **버전 번호와 참조를 함께** 올린다. 변경 이력은 삭제하지 않고 누적 보존한다.

---

## 6. 데이터 모델 (MySQL)

기존 3테이블 유지 + 평가 결과를 18항목 구조로 확장.

**① 상담 마스터** — `상담ID`(PK) · 상담사ID · 고객ID · 상담일시 · 상담유형
**② 상세 대화 내역** — `대화ID`(PK) · `상담ID`(FK) · 대화순서 · 화자 · 타임스탬프(초 단위) · 대화내용
**③ 평가 결과** — `평가ID`(PK) · `상담ID`(FK) · AI모델·버전 · 요약 · 고객만족 · AI피드백 · 원본 JSON

- 통짜 텍스트로 저장하지 말 것 — **발화 단위 + 화자 분리가 문맥 파악·"상담사만 평가" 지시의 핵심.**
- **타임스탬프(초 단위) 필수** — 관리자가 녹취 재검토 시 시간 절약.
- **평가 결과 정규화(제안 🔓):** 원본 JSON은 통째로 `JSON` 컬럼에 보관하되, `항목평가`는 별도 테이블(`평가ID` FK, 항목코드, 충족수준, 획득점수, **근거 대화ID FK**)로 풀어두면 "점수 클릭 → 원문 점프"와 항목별 통계 쿼리가 깔끔하다.
- **🔒 PII:** 고객 성별·연령대 등 개인정보를 다루면 **마스킹/가명화** 적용. 키·비밀번호는 커밋 금지(§12).

---

## 7. 채점 파이프라인 불변식 (🔒 앱이 반드시 지킴)

1. **LLM 산수 미신뢰 → 앱 재계산.** `항목평가`만 신뢰 입력으로 받아 `원점수합 / 적용배점합 = round(…×100,1)`, `위험표시여부`, `상태라벨`을 **코드로** 산출. LLM이 채운 `집계`는 참고용.
2. **N/A는 2단계 트리거.** D1~D5는 *신규판매*에만 ON, **D6는 어떤 권유든 있으면 ON**, 아무 권유 없으면 D 전부 N/A. A2/B2/C3는 조건부. (`docs/N_A_매핑규칙` 단일 기준)
3. **D2 = 3×3 게이트.** 절차개수(`판매정보.확인절차`의 `true` 개수) × 결과(매칭표 적합/부적합/명백한오류). **확인절차 3-bool과 D2 절차개수는 일치해야 한다.**
4. **위험플래그 6규칙 → 상태라벨 3종.** 플래그 1·2·5·6 중 하나 → "불완전판매 의심" / 총점<컷 → "저점수" / 그 외 "정상". 둘 다면 불완전판매 우선. 플래그가 1개 이상이면 `위험표시여부=true`(3·4만 있어도 위험 행 노출).
5. **70점 컷은 하드코딩 금지 → 설정 파라미터.** 7건 소표본 결과이므로 실운영에서 코드 수정 없이 조정 가능하게 `.env`/설정으로 뺀다.
6. **출력은 스키마 ver2로 검증.** 파싱 실패·스키마 위반 시 재시도/에러 처리(§9).

---

## 8. RAG 설계 원칙 (ChromaDB)

- **고정 정답지는 검색하지 말고 프롬프트에 인라인한다.** 매칭표·루브릭·N/A 규칙·상태라벨 규칙은 검색으로 가져오면 **행 누락 → 채점 흔들림** 위험. 채점 프롬프트에 통째로 넣는다(현재 채점 프롬프트 부록에 매칭표·팩트시트가 인라인된 것과 같은 원칙).
- **ChromaDB는 사실 조회 전용.** "상담이 특정 상품 수치·약관 조항을 언급했을 때 그 정오를 확인"하는 용도로 상품설명서·약관을 임베딩·검색(B1·D 근거).
- **규모 주의:** 한빛생명 7종·팩트시트는 매우 작아 **상품설명서까지 인라인해도 무방**할 정도. ChromaDB 분리는 문서가 커질 때의 확장 대비. (🔓 초기엔 인라인, 이후 벡터화 판단)
- 인덱싱은 `scripts/`에서 재현 가능하게(문서 버전 갱신 시 재인덱싱).

---

## 9. LLM 프로바이더 추상화 (Claude / Gemini)

- **프로바이더 교체 가능하게** `lib/llm`에 공통 인터페이스(예: `score(consultation, prompt, facts) → EvalJSON`)를 두고 claude/gemini 구현을 갈아끼운다. 프로바이더 선택은 `.env`.
- **구조화 출력 강제:** JSON만 반환하도록 프롬프트로 지시 + 각 프로바이더의 JSON/structured-output 기능 활용. 응답의 코드펜스(```)·머리말 제거 후 파싱.
- **스키마 검증 → 실패 시 재시도.** 검증 통과분만 저장. 반복 실패는 사람 검토 큐로.
- **결정성:** 채점은 재현성이 중요 → temperature 낮게. (🔓 값은 튜닝)
- 모델명·버전은 `평가메타`에 앱이 주입(모델이 스스로 신뢰성 있게 모름).

---

## 10. 프론트엔드 (Next.js) — QA 담당자 UX (🔓 개괄)

담당자가 "문제 건을 빨리 찾고 근거를 확인"하는 두 화면이 축.

- **목록 화면:** 상담 리스트 + **상태라벨 배지 3종**(불완전판매 의심 🔴 / 저점수 🔴 / 정상 🟢). **점수 낮은 순 정렬**로 애매한 중간 점수도 눈에 띄게. 위험 행은 초록불에 묻히지 않게 노출.
- **상세 화면:** 항목별 점수·충족수준·코멘트. **점수 클릭 → 해당 근거 발화로 원문 점프**(`근거.대화ID` ↔ 상세 대화 내역). 위험플래그·요약·피드백 표시.
- **접근성/현장성:** 담당자 대량 검토를 감안한 밀도·필터·검색(🔓). 대시보드 평균 등 집계 뷰는 후속.

---

## 11. 개발 환경 (Docker) (🔓)

`docker-compose`로 최소 3서비스: **app(Next.js) · mysql · chroma**.

```bash
# 예시(확정 아님) — 실제 커맨드는 구현 시 확정
docker compose up -d           # 전체 기동
docker compose up app          # 앱만
# 초기화: DB 스키마 마이그레이션 → 시드(더미 7건) → ChromaDB 인덱싱
```

- 시크릿·키·컷값은 `.env`(커밋 금지), 템플릿만 `.env.example`.
- 시드에 **더미 상담 7건**을 넣어 로컬에서 즉시 채점·회귀 검증 가능하게.

---

## 12. 코딩 컨벤션 & 보안 (🔒 핵심만)

- **시크릿 금지:** API 키·DB 비밀번호·개인정보를 코드/커밋에 넣지 않는다. 전부 환경변수.
- **PII:** 고객 개인정보 저장 시 마스킹/가명화. 로그에 원문·개인정보 남기지 않기.
- **설정 외부화:** 컷 점수, 프로바이더, 모델명, 임계값은 설정으로.
- **결정성/재현성 우선:** 영리하지만 재현 안 되는 로직보다 단순·검증 가능한 로직.
- 그 외 린트·포맷·네이밍·타입 정책은 🔓 (팀 합의 후 이 절에 추가).

---

## 13. 구조 무결성 원칙 (🔒 불변 — 위반 금지)

이 프로젝트의 **정확성·공정성**을 지키는 규칙. 편의를 위해 어기지 않는다.

- **매칭표는 고정 정답지.** AI 채점 결과에 맞춰 매칭표를 튜닝하면 일치율이 무의미(자기충족). 도메인 버그는 채점 전 1회 수정 후 재고정.
- **골든셋은 사람 손채점만.** AI/페르소나로 골든셋 점수를 만들지 않는다. 채점 전 더미 타당성 수정 OK, **채점 후 수정 금지.**
- **라벨 편향 차단.** AI는 우수/미흡 라벨 없이 채점한다. 라벨이 스며든 골든셋은 AI가 못 따라온다.
- **집계는 앱이(§7-1).** LLM 산수 신뢰 금지.
- **halo 금지.** 전반 인상이 개별 항목 점수로 번지지 않게, 항목은 독립 판정.
- **버전 정합·이력 보존(§5).** 새 버전만 얹고 과거 이력은 지우지 않음.
- **판단 전 원문 대조.** 환각 방지 — 근거는 원문 인용.

> 요약: "매칭표·골든셋·집계·버전"은 시스템의 신뢰 근간이다. 코딩 에이전트는 이 넷을 건드리는 변경을 제안할 때 반드시 사람 확인을 받는다.

---

## 14. 테스트 & 검증

- **골든셋 7건을 회귀 픽스처로.** 파이프라인 변경 시 7건 재채점 → 사람 점수와 비교, **80% 일치율** 목표·이탈 감시.
- **경계 케이스 회귀:** N/A 트리거(권유 유무), D2 3×3 게이트(#1 절차3+적합 / #2 N8→P1 명백한오류 / #7 절차1+적합 부분충족), 위험플래그, 상태라벨.
- **골든셋에 맞춰 채점 로직을 억지로 끼워맞추지 않는다**(§13). 불일치는 원인 분석 후 규칙/프롬프트를 정당하게 개선.

---

## 15. 하지 말 것 (Do NOT)

- ❌ 매칭표를 채점 결과에 맞춰 수정
- ❌ AI로 골든셋 점수 생성 / 라벨을 채점 입력에 노출
- ❌ 집계·상태라벨을 LLM 출력값 그대로 신뢰
- ❌ 70점 컷 하드코딩
- ❌ 고정 정답지(매칭표·루브릭·N/A)를 RAG 검색으로 대체
- ❌ 시크릿·PII를 코드/커밋/로그에 노출
- ❌ 문서 버전 불일치 방치(루브릭·스키마·매칭표·약관)

---

## 16. 열린 질문 / TODO (🔓)

- **채점 프롬프트 rename & 위치 확정** (`prompts/scoring-prompt.md`), 스키마 `title`을 "v1"→"v2"로 맞출지.
- **민원위험(R4)·오정보(R3) 단독 발동 라벨** — 총점 70 초과 + R3/R4만 있는 건이 리스트에서 안 잡히는 케이스. 현재는 `위험표시여부=true`로 위험 행엔 뜸. 실데이터에서 패턴 나오면 라벨 보완 검토.
- **프론트 상세 UX**(필터·검색·대시보드 평균), **인증/권한**(담당자 계정), **대량 배치 채점** 큐.
- **STT 연동/전처리**(과거 통짜 텍스트 화자 분리 에이전트).
- **프로바이더 비교**(Claude vs Gemini 일치율·비용·지연) 후 기본값 결정.
- **컷값·기준 조정 UI**(담당자가 코드 없이 조정).

---

*프로젝트 가이드 — Next.js / Docker / MySQL / ChromaDB / (Claude·Gemini). 정본: 루브릭 v1.5 · 스키마 ver2 · 매칭표 v1.0 · 약관 v2.0. 🔒는 불변 규칙, 🔓는 열린 결정 사항.*

